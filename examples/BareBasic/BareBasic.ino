
#define RE 7
#define RA 10
#define RB 9
#define LE 8
#define LA 2
#define LB 3

void setup() {
  pinMode(RE, OUTPUT);
  pinMode(RA, OUTPUT);
  pinMode(RB, OUTPUT);
  pinMode(LE, OUTPUT);
  pinMode(LA, OUTPUT);
  pinMode(LB, OUTPUT);
}

void loop() {
  // put your main code here, to run repeatedly:

}
